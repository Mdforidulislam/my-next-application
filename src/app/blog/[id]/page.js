import React from 'react';

const blogContent = ({params,searchParams}) => {
    console.log(searchParams);
    console.log(params);
    return (
        <div>
            <h1>this is my pages </h1>
        </div>
    );
};

export default blogContent;